// under GPL v2

//set env vars
//***COMMENDED OUT FOR THE TIME BEING and changing the default directly on SDL lib to avoid a gcc build quirky behavior
void set_envs(void) {
	//Sys_SetEnv("SDL_DISABLE_LOCK_KEYS", "1"); //don't lock caps and num lock
}	
